# Cloudflare Pages _redirects
# Note: Trailing slash normalization is handled automatically by Astro's
# trailingSlash: 'never' config + Cloudflare Pages — no manual rules needed.
# Adding duplicate rules here causes ERR_TOO_MANY_REDIRECTS loops.
